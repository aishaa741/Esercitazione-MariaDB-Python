{
  "name": "Ambiente Python e MariaDB",
  "image": "mcr.microsoft.com/devcontainers/python:3",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mariadb-server",
  "postStartCommand": "sudo service mariadb start"
}